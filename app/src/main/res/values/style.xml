<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="CustomDropDownMenuStyle" parent="@style/Widget.MaterialComponents.AutoCompleteTextView.FilledBox">
<!--        <item name="android:popupBackground">@android:color/transparent</item>-->
        <item name="android:scrollbars">none</item>
    </style>

    <style name="CustomDatePickerStyle" parent="Theme.MaterialComponents.Dialog">
        <item name="android:datePickerStyle">@style/CustomDatePicker</item>
        <item name="android:colorControlNormal">@color/crimson</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="windowMinWidthMinor">@dimen/date_picker_width</item>
        <item name="android:windowBackground">@drawable/all_corners_radious_with_padding_drawable</item>

    </style>

    <style name="CustomDatePicker" parent="android:Widget.Material.DatePicker">
        <item name="android:datePickerMode">spinner</item>

    </style>

</resources>
